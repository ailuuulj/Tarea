# TP4\Ej1
#Escribir un programa que lea dos números enteros desde el teclado, los sume y
#muestre el resultado por pantalla.	
.data 	
salida: 	.asciz "\nIngrese numero: " 
fin:		.asciz "\nEl resultado de la suma de los dos valores ingresados es: "

##a7 = 5 ? leer entero\
##a7 = 1 ? imprimir entero

.text

main:

	# Mostrar mensaje
	li a7, 4
	la a0, salida
	ecall
	
	#guardar num ingresado en t1
	li a7, 5 
	ecall	 # el numero queda en a0
	
	#transferimos (t1<--a0)
	mv t1, a0
	
	#muestra msj
	li a7, 4
        la a0, salida
        ecall
	
	li a7, 5 
	ecall#	 # el numero queda en a0
	mv t2, a0
		
	#suma
	add t3, t2, t1 # a0<- a0+t1
	
	#mostrar mensaje final
	li a7, 4
	la a0, fin
	ecall
	
	li a7, 1
	addi a0, t3, 0
	ecall
	